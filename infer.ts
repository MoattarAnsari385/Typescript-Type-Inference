//-----------------------------------Type Inference-------------------------------------- 


let firstName = "Moattar"; // The complier infers the type string for the variable name.
let age = 17; // The compiler infers the type number for variable age.
let isValid = true; //The compiler infers the type boolean the variable is valid
